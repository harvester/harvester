# descheduler

Runs the Kubernetes descheduler to rebalance VM workloads across nodes (virtual-machine-auto-balance).

- **Stage:** `experimental`
- **Built-in:** yes (packaged into the ISO; managed via `addons/descheduler/addon-template.yaml`)
