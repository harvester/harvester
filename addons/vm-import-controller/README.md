# vm-import-controller

Imports VMs from external providers (e.g. VMware) into Harvester.

- **Stage:** `ga`
- **Built-in:** yes (packaged into the ISO; managed via `addons/vm-import-controller/addon-template.yaml`)
