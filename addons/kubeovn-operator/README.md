# kubeovn-operator

Deploys the Kube-OVN CNI operator as an alternative networking backend.

- **Stage:** `experimental`
- **Built-in:** yes (packaged into the ISO; managed via `addons/kubeovn-operator/addon-template.yaml`)
