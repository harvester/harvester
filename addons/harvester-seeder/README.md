# harvester-seeder

Automates BMC-based node inventory and metadata seeding for bare-metal provisioning.

- **Stage:** `ga`
- **Built-in:** yes (packaged into the ISO; managed via `addons/harvester-seeder/addon-template.yaml`)
