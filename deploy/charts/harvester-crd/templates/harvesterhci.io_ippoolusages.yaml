---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    {}
  name: ippoolusages.harvesterhci.io
spec:
  group: harvesterhci.io
  names:
    kind: IPPoolUsage
    listKind: IPPoolUsageList
    plural: ippoolusages
    shortNames:
    - ipusage
    - ipusages
    singular: ippoolusage
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.cidr
      name: CIDR
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              cidr:
                description: CIDR defines the address pool managed by this resource.
                type: string
              reservedIPCount:
                default: 8
                description: ReservedIPCount preserves the first N allocatable IPs
                  in the CIDR for special use.
                type: integer
            required:
            - cidr
            type: object
          status:
            properties:
              allocations:
                additionalProperties:
                  properties:
                    allocatedAt:
                      description: AllocatedAt is when this IP was first assigned
                        to the current resource.
                      format: date-time
                      type: string
                    lastUpdateTime:
                      description: LastUpdateTime is when the allocation entry was
                        last reconciled.
                      format: date-time
                      type: string
                    resource:
                      description: Resource identifies the current owner of the IP.
                      properties:
                        apiVersion:
                          type: string
                        kind:
                          type: string
                        name:
                          type: string
                        namespace:
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      type: object
                  required:
                  - resource
                  type: object
                description: Allocations tracks which resource is currently using
                  each allocated IP.
                type: object
              conditions:
                description: Conditions reports the reconciliation status for the
                  pool.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    lastUpdateTime:
                      description: The last time this condition was updated.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition
                      type: string
                    reason:
                      description: The reason for the condition's last transition.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type of the condition.
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              reservedIPs:
                description: ReservedIPs lists the IPs currently preserved from normal
                  allocation.
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
