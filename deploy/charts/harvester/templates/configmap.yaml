apiVersion: v1
kind: ConfigMap
metadata:
  name: kubevip
  namespace: kube-system
data:
{{- if and (eq .Values.service.harvester.vipMode "static") .Values.service.harvester.vip }}
  cidr-harvester-system: {{ .Values.service.harvester.vip }}/32
{{- end }}
